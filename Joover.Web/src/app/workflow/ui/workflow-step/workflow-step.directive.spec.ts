import { WorkflowStepDirective } from './workflow-step.directive';

describe('WorkflowStepDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkflowStepDirective();
    expect(directive).toBeTruthy();
  });
});
